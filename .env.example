# No secrets needed in the frontend — all Zoho calls go through the backend proxy.
# Copy to .env.local and set only if you override the default proxy target.

# Override backend URL if not using the Vite proxy (e.g. production)
VITE_API_BASE_URL=https://cckart-backend-production.up.railway.app
